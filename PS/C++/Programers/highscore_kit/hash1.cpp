#include <string>
#include <vector>

using namespace std;

string solution(vector<string> p, vector<string> c) {
    string answer = "";
    int i, j;
    for(i=0;i<p.size();++i)
    {
        for(j=0;j<c.size();++j)
        {
            
        }
    }

    return answer;
}